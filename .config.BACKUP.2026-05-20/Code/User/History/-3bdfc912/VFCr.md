# Data and Types

## Data types

the latest ECMAScript standard definses eightt data types:

1. Boolean. True and False.
2. null. A special keyword denoting a null value. (Because a JavaScript is case sensitive, `null` not is same as `Null`, `NULL`, or same other variant ).
3. undefined. A top-level property whose value is not defined.
4. Number. An integer or floating point number. For example `32` or `3.14159`.
5. BigInt. An integer with arbitrary precision. For example: `927923947738327n`.
6. String. A sequence of characters that represent a text value. For example: "Howdy"
