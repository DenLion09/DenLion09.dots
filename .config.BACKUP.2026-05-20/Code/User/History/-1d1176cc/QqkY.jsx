import { useState } from "react";

const Spinner = ({ prop }) => {
  const [isLOading, setIsLoading] = useState("true");
  return (
    <div>
      { //animacion}
    </div>
  );
};

export default Spinner;
