import { useState } from "react";

const Spinner = ({ prop }) => {
  const [isLOading, setIsLoading] = useState("true");
  return (
    <div>
      <div>aqui va la animacion </div>
    </div>
  );
};

export default Spinner;
